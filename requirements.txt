# Core
flask==3.1.3
werkzeug==3.1.8
jinja2==3.1.6
click==8.4.1

# Data
feedparser==6.0.12
requests==2.34.2
python-dotenv==1.2.2
truststore==0.10.4   # validate HTTPS against the OS cert store (TLS-inspecting AV/proxies in local dev)

# AI
anthropic==0.107.1

# Production WSGI server (not needed for local dev)
gunicorn>=22.0
gevent>=24.0
